//pg84

#include <stdio.h>

int main()
{
	/* local variable declaration */
	int a,b;
	int c;

	/* actual initialization */
	a = 10;
	b = 20;
	c = a + b;

	printf("Value of A = %d, Value of B = %d, Value of C = %d\n", a, b, c);

	return 0;
}